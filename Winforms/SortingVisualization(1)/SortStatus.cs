namespace SortingVisualization {

    // generate and sort status
    public enum STATUS {
        GENERATING,
        GENERATED,
        SORTING,
        SORTED
    }

    // set only one or poli color
    public enum COLOR {
        MONO,
        POLI
    }
}
